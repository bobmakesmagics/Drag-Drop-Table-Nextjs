import React from 'react';
import { DragTable } from '@/components/DragTable';

const Table = () => {
  return (
    <div className="flex justify-center p-32 bg-white min-h-screen">
      <DragTable />
    </div>
  );
};

export default Table;
